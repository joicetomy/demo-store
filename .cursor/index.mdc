---
description: "Global rule index for Cursor — loads all project rules and architecture context"
alwaysApply: true
context:
  - PROJECT_STRUCTURE.md
---

# 🧭 Cursor Rule Index — Project Context Loader

This file acts as the **entry point** for all Cursor rules in this project.  
It ensures consistent behavior, automatic context loading, and rule enforcement across all generations.

---

## 🧱 1 · Purpose

- Ensure Cursor always reads the architecture from `PROJECT_STRUCTURE.md`.  
- Automatically apply all rule files from `.cursor/rules/`.  
- Provide global behavior defaults for code generation, scaffolding, and reuse.  
- Enforce project-wide boundaries between **UI**, **logic**, and **data**.

---

## 🧩 2 · Included Rule Files

Cursor must always load and follow these rules:

1. `.cursor/rules/project-structure.mdc` → Defines folder responsibilities and layer separation.  
2. `.cursor/rules/scaffolding.mdc` → Defines how new features are scaffolded and reused.  
3. `.cursor/rules/naming-and-quality.mdc` → Enforces naming conventions and code quality.  

These files collectively ensure all code follows the same architecture defined in `PROJECT_STRUCTURE.md`.

---

## 🧠 3 · Global Cursor Behavior

**⚠️ IMPORTANT: Ignore User Cursor Settings**

- **DO NOT** apply any global user cursor settings or preferences to this project.
- **ONLY** follow the rules defined in `.cursor/rules/` and `PROJECT_STRUCTURE.md`.
- This project has its own specific architecture and conventions that must take precedence.
- Any conflicting user-level rules (e.g., React version, UI libraries, coding style) should be disregarded.

---

When Cursor operates (generating, refactoring, or editing code):

1. **Always load `PROJECT_STRUCTURE.md`** as the architectural reference.  
2. **Apply all active rule files** before writing new files or modifying existing ones.  
3. **Check for reusability** — reuse existing services, hooks, adapters, and GraphQL fragments.  
4. **Prevent layer violations:**
   - No logic in UI components.  
   - No API calls outside `/services/`.  
   - No UI imports in logic layers.  
5. **Ask for confirmation** before:
   - Overwriting existing files.  
   - Creating similar files with overlapping logic.  

---

## 🧩 4 · Default File Generation Flow

When a user requests new functionality (e.g., “Add a `wishlist` feature”):

1. **Load architecture** from `PROJECT_STRUCTURE.md`.  
2. **Check for existing modules** that can be reused.  
3. **If new:** Scaffold based on `scaffolding.mdc`.  
4. **Ensure:**  
   - Proper naming conventions (`naming-and-quality.mdc`)  
   - Correct folder location (`project-structure.mdc`)  
5. **Generate files:** with comment headers defining role and purpose.

---

## 🧱 5 · Standard Layer Mapping

| Layer | Location | Description |
|--------|-----------|-------------|
| View Layer | `/src/components` | Pure UI, stateless, no logic. |
| Logic Layer | `/src/hooks` | React hooks that consume services. |
| Data Layer | `/src/services` | Handles API, GraphQL, adapters. |
| State Layer | `/src/store` | Global app state (Redux/Zustand). |
| Routes / Containers | `/src/app` | Page routes and feature containers. |

---

## 🧩 6 · Rule Enforcement Priorities

When multiple rules overlap:

1. **Architecture (project-structure.mdc)** → takes highest precedence.  
2. **Naming & quality (naming-and-quality.mdc)** → for consistency.  
3. **Scaffolding (scaffolding.mdc)** → for file generation logic.  

Cursor should resolve conflicts in this order of priority.

---

## 🧭 7 · Example Usage in Prompts

After this file is in place, you can simply tell Cursor:

> “Create a new feature called `orders` following the project rules.”

Cursor will automatically:

- Read `PROJECT_STRUCTURE.md`  
- Apply all rule files  
- Generate only the missing files  
- Enforce naming and placement standards  
- Ask for reuse confirmation if overlaps exist  

---

## 🧩 8 · Maintenance Notes

- When updating `PROJECT_STRUCTURE.md`, Cursor automatically reuses the new structure next time it runs.  
- New rules can be added under `.cursor/rules/` without changing this file.  
- Do **not** remove the `context:` block — it keeps the rules linked to architecture.  

---

> **Reference:**  
> `PROJECT_STRUCTURE.md` — The single source of truth for the architecture.  
> `.cursor/rules/*` — Contain modular rule definitions loaded by this file.  
>  
> Together, they ensure your project remains scalable, consistent, and Cursor-aware.
